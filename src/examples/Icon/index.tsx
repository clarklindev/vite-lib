import { Icon } from '../../../lib/components';
import { ShoppingCartIcon } from '../../../lib/icons';

const IconExample = () => {
    return (
        <Icon size="M">
            <ShoppingCartIcon />
        </Icon>
    );
};

export default IconExample;
