import { Icon } from '@/components/Icon';
import { ShoppingCartIcon } from '@/icons';

const IconExample = () => {
    return (
        <Icon size="M">
            <ShoppingCartIcon />
        </Icon>
    );
};

export default IconExample;
